import React from 'react'

const Title = ({title}) => {
    return (
      <div className="mb-12 font-semibold text-3xl">
        <h2>{title}</h2>
        <div />
      </div>
    );
}

export default Title;